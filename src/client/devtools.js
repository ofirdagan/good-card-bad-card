import React, {PureComponent} from 'react';
export default class Devtools extends PureComponent {
  render() {
    return (
      <h1 className={'devtools'}>
        For this demo to work well, open your dev tools
      </h1>
    );
  }
}
